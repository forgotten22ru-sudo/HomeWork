# Дипломный проект тестирования UI и API

Автоматизированные тесты на **Playwright (JavaScript)** с отчетами через **Allure** и генерацией сценариев через **codegen**.  
Этот проект создан для практики и закрепления навыков написания UI и E2E тестов для веб‑приложений.

---

## 📌 Основные возможности

В проекте реализовано:

- 💻 UI тесты для браузерных сценариев  (тестирование сервиса [Realworld](https://realworld.qa.guru ))
- 🧪 API тесты через Playwright Request API (тестирование сервиса [Apichallenges](https://apichallenges.herokuapp.com/gui/challenges) )
- 🧬 Faker для генерации тестовых данных 
- 📊 Отчетность через **Allure Reports**
- 🎭 Запуск тестов в разных режимах (в т.ч. UI‑интерфейс)


---

## 🧰 Технологии

Проект использует:

| Технология | Назначение |
|------------|------------|
| `@playwright/test` | Фреймворк для автоматизированного тестирования браузеров |
| `allure-playwright` | Плагин для создания Allure отчетов |
| `@faker-js/faker` | Генерация случайных тестовых данных |
| Node.js | Среда выполнения JavaScript |

---

## 🧪 Что тестируется

### UI тесты:

- Авторизация и регистрация

- Редактирование профиля пользователя

- Создание и редактирование статей

- Навигация по страницам


### API тесты:

- Работа с todos (создание, получение, обновление, удаление)

- Проверка ответов сервера, статусов и структуры JSON

- Использование токенов и заголовков

---

## ⚡ Быстрый старт

### 1. Клонировать репозиторий
```
git clone https://github.com/forgotten22ru-sudo/HomeWork.git
cd HomeWork
```
### 2. Установить зависимости
```
npm install
```
### 3. Установить браузеры Playwright
```
npx playwright install
```
---

## ▶️ Скрипты и запуск

| Скрипт                 | Что делает                                 |
| ---------------------- | ------------------------------------------ |
| `npm test`             | Запускает все тесты                        |
| `npm run ui`           | Открывает UI‑режим запуска Playwright      |
| `npm run codegen`      | Генерация теста через `playwright codegen` |
| `npm run report`       | Открывает HTML‑отчёт Playwright            |
| `npm run allure`       | Запускает Allure сервер для отчета         |
| `npm run reportallure` | Генерирует Allure отчет и виджеты          |

---

## 📊 Отчеты

После запуска тестов можно:

✔️ Посмотреть интерактивный HTML‑отчет Playwright:

npm run report


✔️ Сформировать Allure‑отчет:

npm run allure


или собрать полный отчет для публикации:

npm run reportallure

---

## 📁 Структура проекта

| Путь                                   | Описание                                                      |
| -------------------------------------- | ------------------------------------------------------------- |
| `package.json`                         | Зависимости проекта и npm-скрипты 🛠️                         |
| `playwright.config.js`                 | Конфигурация Playwright (браузеры, репортеры, таймауты)       |
| `allure-results/`                      | Директория с результатами тестов для Allure ✨ (Создается после запуска тестов)                |
| `tests/`                               | Папка с тестами                                               |
| `tests/api.spec.js`                    | API-тесты (работа с `todos`, `challenges`)                    |
| `tests/ui.spec.js`                     | UI-тесты (работа со статьями и профилем)                      |
| `src/`                                 | Исходный код вспомогательных модулей                          |
| `src/services/`                        | Сервисы для работы с API                                      |
| `src/services/api.services.js`         | Класс `Api` с сервисами (`challenger`, `todos`, `challenges`) |
| `src/helpers/`                         | Вспомогательные функции и генераторы данных                   |
| `src/helpers/builders/`                | Билдеры для генерации тестовых данных                         |
| `src/helpers/builders/todo.builder.js` | Билдер для создания объекта `todo`                            |
| `src/helpers/builders/index.js`        | Экспорт всех билдера (например `user`, `article`)             |
| `src/helpers/fixtures/`                | Playwright-фикстуры для UI-тестов (auth, setup и т.п.)        |
| `scripts/`                             | Скрипты для обработки отчетов                                 |
| `scripts/allure2-widgets-summary.cjs`  | Генерация виджетов для Allure                                 |

---

## 📊 CI/CD интеграция

- Все тесты запускаются автоматически при push в ветку main
- Либо можно запустить запуск тестов вручную через переход 
вкладка Actions → Playwright Tests → Run workflow
- После каждого запуска Actions формируется Allure отчет в формате html
### Пример выполненых тестов

<img width="1351" height="701" alt="image" src="https://github.com/user-attachments/assets/bed563ea-2a7c-45b8-9002-5115c8ba69ff" />

### Интеграция в Allure TestOps

CI/CD Настроен на передачу результатов тестирования в формате отчета Allure в Allure TestOps, где после выполнения тестов можно будет с ними ознакомиться

### Пример выполненых тестов в Allure TestOps

<img width="1794" height="655" alt="image" src="https://github.com/user-attachments/assets/b7de7173-5b93-4afc-9f75-27b93d182bab" />

### Формирование отчетов в Telegram

Благодаря [allure-notifications](https://github.com/qa-guru/allure-notifications?tab=readme-ov-file) после выполнения всех тестов
происходит рассылка результатов тестов в Телеграм, рассылка содержит диаграмму успешных выполнений тестов, а так же ссылку, 
перейдя по которой можно ознакомиться с отчетом

При работе с Allure 3 необходимо использовать скрипт **\temp\scripts\allure2-widgets-summary.js**
Для работы со скриптом в репозитории приложен README **\temp\scripts\README.md**

### Сообщение с отчетом в Telegram

<img width="688" height="567" alt="image" src="https://github.com/user-attachments/assets/2e47591f-2b3e-4155-be81-71b38f4a25de" />

### Пример просмотра Allure отчета

<img width="1576" height="826" alt="image" src="https://github.com/user-attachments/assets/f679b5a5-31b7-4b9f-922a-7427cfb6b813" />

<img width="1808" height="884" alt="image" src="https://github.com/user-attachments/assets/94dc6cc5-04c3-487f-b855-f795aefbd48e" />




